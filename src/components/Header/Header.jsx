import Nav from '../Nav/Nav'

const Header = () => {
  return (
    <header className="sticky w-full top-0 z-20 shadow-header bg-emptiness border-blackOut border-b-2 rounded">
      <Nav></Nav>
    </header>
  )
}

export default Header
